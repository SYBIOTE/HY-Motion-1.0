# One-shot local install (same stack as Docker; no Gradio / FBX SDK).
# For Docker layers see requirements-torch.txt, requirements-base.txt, requirements-api.txt.

-r requirements-torch.txt
-r requirements-base.txt
-r requirements-api.txt
